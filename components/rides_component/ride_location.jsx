import React from "react";

const RideLocation = ({ value }) => {
    return (
        <div className="text-white bg-black-dark rounded-full p-1.5 text-xs">
            {value}
        </div>
    );
};

export default RideLocation;
